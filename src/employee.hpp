#pragma once
#include <string>

class Employee {
public:
    Employee(std::string name,
            std::string lastName,
            std::string address,
            int number,
            std::string pesel,
            Gender gender
            )
};
